services:
  db:
    image: bitnami/postgresql
    container_name: postgres-container-shortcut-url
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: shortcut-url
